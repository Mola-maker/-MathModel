"""Shared constants and reference data for all agent prompts."""

# ── Language selection rule (injected into every agent) ──
LANGUAGE_SELECTION_RULE = """
## 语言确认（最高优先级，所有 Agent 在首次输出前必须遵守）
系统已从用户配置中读取语言设置：{language}
- 若为 "zh"：全文使用中文撰写（论文正文、图注、代码注释均为中文）
- 若为 "en"：全文使用英文撰写（MCM 官方要求，推荐竞赛提交时使用）
- 若未设置：默认英文
全程严格保持该语言，禁止混用。
"""

# ── Formula calculation rule (anti-hallucination, highest priority) ──
FORMULA_CALCULATION_RULE = """
## 公式与计算规范（最高优先级，违反则拒绝输出）

### 三层结构强制规则
每一个公式必须包含以下三层，缺一不可：

**第一层：符号定义与公式**
使用 LaTeX 给出带变量说明的标准公式。

**第二层：代入真实数据的数值计算过程（禁止省略）**
必须将代码运行所得的真实数值代入公式，逐步展示计算过程。
禁止写"代入数据得..."而不给出具体数字。
禁止虚构任何数值，所有数字必须来自代码的实际输出。

**第三层：结果解释**
说明计算结果的物理意义或实际意义，对照参考标准或基线进行解读。

### 禁止行为（AI 幻觉防范）
- 禁止凭空给出参数值（必须说明来源：代码输出/文献引用/数据统计）
- 禁止只写公式不计算（必须代入真实数据）
- 禁止虚构引用（所有参考文献必须真实可查，有完整 DOI 或 URL）
- 禁止圆整实验数据（保留代码输出的原始精度）
- 禁止在 Results 节给出与代码输出不符的数值

### 正确示例
```
目标函数：Z = Σ wᵢxᵢ

代入数据（来自 vol/outputs/solver_results.json）：
  w₁ = 0.35（PM2.5权重，AHP层次分析得），x₁ = 82.3（站点A实测均值）
  w₂ = 0.28（NO₂权重），x₂ = 45.6
  w₃ = 0.37（O₃权重），x₃ = 61.2

计算过程：
  Z = 0.35×82.3 + 0.28×45.6 + 0.37×61.2
    = 28.805 + 12.768 + 22.644
    = 64.217

结果解释：综合污染指数 Z = 64.217，落入 WHO 中等污染区间（50-75），
表明该区域需要关注但尚未达到预警阈值。
```
"""

# ── Award paper experience (national prize patterns) ──
AWARD_PAPER_REFERENCE = """
## 历届国奖作品经验参考

### 国奖论文结构特征
- 模型复杂度：主模型 + 验证模型 + 扩展模型（通常 2-3 个互补）
- 创新点标注：必须明确说明与经典方法的差异（"本文在 XXX 基础上引入 YYY"）
- 敏感性分析：所有国奖论文必含，变量扰动范围 ±10%~±30%
- 可视化密度：每道子问题平均 3-5 张图表
- 假设体系：每条假设必须有合理性说明，核心假设不超过 8 条
- 计算过程：关键结论必须配有完整数值推导（这是区分国奖与普通作品的核心差距）

### 国奖常用高分模型组合
| 问题类型 | 高分组合 | 创新加分点 |
|---------|---------|-----------|
| 优化类 | NSGA-II + 敏感性分析 | Pareto 前沿可视化 |
| 预测类 | LSTM + ARIMA 对比 + 置信区间 | 残差分析 + Bootstrap CI |
| 评价类 | AHP-TOPSIS + 熵权法交叉验证 | 权重一致性检验（CR<0.1）|
| 网络类 | 复杂网络 + 社区发现 + 级联故障 | 鲁棒性分析 |
| 统计类 | 多元回归 + SHAP 值 + VIF 共线性检验 | 因果推断说明 |

### 写作风格参考
- 摘要必须包含模型名称和关键结论数值（不能只说"建立了模型"）
- 每节开头一句话说明本节目标，结尾一句话总结结论
- 图表描述采用"Figure X shows/reveals/demonstrates"，后接具体数据
"""

# ── Reference retrieval rule ──
REFERENCE_RETRIEVAL_RULE = """
## 参考文献检索规范

### 检索策略（写作手执行）
每道子问题必须完成以下检索，关键词从题目自动提取：
- 数据库优先级：Web of Science > Scopus > Google Scholar > CNKI（中文题目）
- 检索式模板：[主题词] AND ("mathematical model" OR "optimization") AND (year:2020-2024)
- 引用数筛选：优先引用被引 >50 次的文献作为方法基础

### 引用要求
- 每道子问题最少引用 5 篇（1 篇综述 + 2 篇方法论文 + 2 篇应用论文）
- 全文总引用数 ≥ 20 条（MCM 评委预期）
- BibTeX 格式：所有引用必须导出标准 BibTeX，写入 paper/references_draft.bib

### 禁止行为
- 禁止虚构任何作者名、期刊名、卷号、页码、DOI
- 禁止引用不存在的论文
- 禁止重复引用同一文献多次（每篇文献只引用一次）
- 若无法确认文献真实性，改为引用公开数据集或官方报告
"""

# ── Academic color palette ──
VIZ_COLORS = {
    "primary": "#2E5B88",
    "secondary": "#E85D4C",
    "tertiary": "#4A9B7F",
    "neutral": "#7F7F7F",
    "light": "#B8D4E8",
}

# ── Algorithm guide (from MathModel reference) ──
ALGORITHM_GUIDE = """
## 十类核心算法

1. **蒙特卡洛方法** — 随机抽样/模拟
2. **数据拟合、参数估计、插值** — 回归/最小二乘/样条
3. **线性规划、整数规划、二次规划** — LP/IP/QP
4. **图论算法** — Dijkstra/Floyd/Prim/Kruskal/最大流
5. **动态规划、回溯、分治** — DP/递归搜索
6. **模拟退火、神经网络、遗传算法** — 元启发式优化
7. **网格搜索与穷举** — 参数调优/暴力搜索
8. **连续→离散转换** — 离散化/分箱
9. **数值分析** — ODE/PDE 数值解/有限差分/有限元
10. **图像/图形处理** — 滤波/形态学/特征提取
"""

# ── Model combination reference ──
MODEL_COMBINATIONS = """
| 问题类型 | 推荐组合 | 说明 |
|---------|---------|------|
| 综合评价 | AHP + TOPSIS | AHP定权重，TOPSIS排序 |
| 预测+不确定性 | XGBoost + Bootstrap/MCMC | 预测+置信区间 |
| 方案优选 | AHP + 熵权法 + TOPSIS | 主客观结合更有说服力 |
| 风险评估 | Logistic回归 + 蒙特卡洛 | 概率+模拟 |
| 文本分析 | TF-IDF + LDA + 情感分析 | 特征提取+主题+情感 |
| 时空预测 | ARIMA/LSTM + 空间聚类 | 时间+空间维度 |
| 投资优化 | 预测模型 + 动态规划/遗传算法 | 预测+优化 |
| 效应分析 | DID/回归 + SHAP值分析 | 因果+解释 |
"""
